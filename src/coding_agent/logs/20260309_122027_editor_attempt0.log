=== EDITOR AGENT ===
Timestamp: 20260309_122027
Max tokens: 20000

=== PROMPT ===
You are Editor, a code modification agent. You receive a file and a task, then produce the modified content.

CRITICAL RULES:
1. Output ONLY valid code
2. Preserve all imports, exports, and structure
3. Only change what is necessary for the task
4. Keep indentation and formatting consistent
5. You MUST always wrap the complete modified file between markers:
   - A single line with ---FILE_START---
   - A single line with ---FILE_END---

Response format:
First, explain what changes you will make in one line.
Then output the complete modified file between markers exactly as shown:
---FILE_START---
<complete file content>
---FILE_END---

---

Now process:
Task: Rename function "calculateTotal" to "computeSum"
File: playground/math.js
Content:
1|function calculateTotal(x) {
2|    return x;
3|}
5|module.exports = { calculateTotal };



=== COMPLETION (streaming) ===
I will rename the function "calculateTotal" to "computeSum" and update the export accordingly.

---FILE_START---
function computeSum(x) {
    return x;
}

module.exports = { computeSum };
---FILE_END---

=== END ===
   return sum;
}
```

[variable] exports
```
module.exports = { calculateTotal };
```

Renaming function declaration and export.
{"changes": [{"original": "function calculateTotal(items) {", "new_content": "function computeSum(items) {"}, {"original": "module.exports = { calculateTotal };", "new_content": "module.exports = { computeSum };"}]}

---

Example 2:
Task: Rename function "calculateTotal" to "computeSum"
File: ./src/app.js
Chunks:
[variable] import
```
const { calculateTotal } = require('./math');
```

[function] checkout
```
function checkout(cart) {
    const total = calculateTotal(cart);
    return { total, items: cart.length };
}
```

Renaming import and function call.
{"changes": [{"original": "const { calculateTotal } = require('./math');", "new_content": "const { computeSum } = require('./math');"}, {"original": "const total = calculateTotal(cart);", "new_content": "const total = computeSum(cart);"}]}

---

Example 3 (multi-line change):
Task: Fix syntax error in function
File: ./src/util.js
Previous error: Missing closing brace
Chunks:
[function] getData
```
function getData() {
    return {
        value: 42
}
```

Fixing missing brace by replacing entire function.
{"changes": [{"original": "function getData() {\n    return {\n        value: 42\n}", "new_content": "function getData() {\n    return {\n        value: 42\n    };\n}"}]}

---

Now process:
Task: Rename function/variable "max" to "minimum"
Rename ONLY identifiers named exactly "max" (function declarations, calls, imports, exports)
DO NOT rename: constants (MAX_ITEMS), variables with similar names (max_items), strings, comments
File: utils.py
Chunks:
[variable] MAX_ITEMS
```
MAX_ITEMS = 100
```

[variable] max_items
```
max_items = 50
```

[function] max
```
def max(a, b):
    return a if a > b else b
```

[variable] result
```
result = max(MAX_ITEMS, max_items)
```



=== COMPLETION (streaming) ===
{"changes": [{"original": "def max(a, b):", "new_content": "def minimum(a, b):"}, {"original": "result = max(MAX_ITEMS, max_items)", "new_content": "result = minimum(MAX_ITEMS, max_items)"}]}

=== END ===
{ min, MAX_DEPTH };"}]}

=== END ===
